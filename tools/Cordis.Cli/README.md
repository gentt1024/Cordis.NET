# Cordis.Tool

.NET tool for validating and previewing Cordis.NET entry configuration. After installing a published package, use `cordis validate <path>` or `cordis preview <path>`. Until publication, run the tool project from this repository.

Cordis.NET is independently maintained and is not an official project of DeepSeek, Cordiverse, or Microsoft.
